
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/asbp
BETTER_AUTH_SECRET=changeme
BETTER_AUTH_URL=http://localhost:3000

# Client-side app URL (used by auth-client for API calls)
# If not set, falls back to window.location.origin in browser
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database connection pool settings (optional)
# DB_POOL_MAX=10
# DB_POOL_MIN=2

# Encryption key for sensitive settings (optional)
# If not set, derived from DATABASE_URL
# Generate with: openssl rand -hex 32
# ENCRYPTION_KEY=your-64-character-hex-string

# Redis URL for optional second-level cache (Phase 2)
# REDIS_URL=redis://localhost:6379
