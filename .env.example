
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/asbp
BETTER_AUTH_SECRET=changeme
BETTER_AUTH_URL=http://localhost:3000

# Public app URL (optional, overrides BETTER_AUTH_URL for SEO/links)
# Used for: sitemap, robots.txt, Open Graph meta tags, RSS feed, email links
# Server-side fallback: BETTER_AUTH_URL > localhost:3000
# Client-side fallback: window.location.origin > localhost:3000
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# Database connection pool settings (optional)
# DB_POOL_MAX=10
# DB_POOL_MIN=2

# Encryption key for sensitive settings (optional)
# If not set, derived from BETTER_AUTH_SECRET
# Generate with: openssl rand -hex 32
# ENCRYPTION_KEY=your-64-character-hex-string

# Redis cache (optional)
# When set, uses Redis for caching settings, posts, and navigation
# When not set, falls back to Next.js in-memory cache
# REDIS_URL=redis://localhost:6379
